//javascript code

for (var teller = 1; teller <= 5; teller++) {
    console.log(teller);
    if (teller == 5 || teller == 5) {
        console.warn("het einde is bereikt!")
    }
}

for (var teller = 5; teller <= 1; teller++) {
    console.log(teller);
    if (teller == 1 || teller == 1) {
        console.warn("het begin is het begin")
    }
}